version: "2.4"
services:
    db:
        extends:
            file: common.yaml
            service: db
        container_name: db
        restart: unless-stopped
        env_file:
            - .docker/db-creation.env

volumes:
    db:
